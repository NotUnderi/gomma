services:
  db:
    image: postgres:16
    container_name: postgres
    restart: unless-stopped
    env_file:
      - ./.env
    ports:
      - "5432:5432"
    volumes:
      - gomma_postgres_data:/var/lib/postgresql/data

volumes:
  gomma_postgres_data:
